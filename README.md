# 🎙️ AI‑чат с распознаванием речи и анализом текста

Этот проект — настольное приложение на **Python + PyQt6**, которое использует **Whisper** для распознавания речи и **Ollama (LLM)** для анализа текста и ответов на вопросы.

Приложение умеет:

* записывать звук с микрофона;
* принимать аудиофайлы;
* транскрибировать речь в текст;
* отвечать на вопросы по полученному или загруженному тексту.

---

## 🚀 Основные возможности

* 🎧 **Запись с микрофона**
  Пользователь может записать голос напрямую из приложения.

* 📂 **Загрузка аудиофайла**
  Поддержка файлов (wav / mp3 / m4a и др., зависит от Whisper).

* 📝 **Транскрибация**
  Аудио преобразуется в текст с помощью модели **Whisper**.

* 🤖 **AI‑чат**
  Текст отправляется в **Ollama**, где языковая модель:

  * отвечает на вопросы;
  * объясняет содержание;
  * делает краткое резюме.

* 🖥️ **Графический интерфейс (PyQt6)**
  Удобное окно с кнопками записи, загрузки и чатом.

---

## 🧠 Используемые технологии

* **Python 3.10+**
* **PyQt6** — графический интерфейс
* **Whisper / faster-whisper** — распознавание речи
* **Ollama** — локальные LLM-модели
* **FFmpeg** — обработка аудио

---

## 🎙️ Модели Whisper: сравнение и требования к железу

### ⏱️ Скорость работы (1 минута аудио)

> Значения усреднённые, для **CPU Ryzen 5 / Intel i7**, faster-whisper, без GPU.

| Модель     | Время на 1 мин аудио | Комментарий              |
| ---------- | -------------------- | ------------------------ |
| **tiny**   | ~5–10 сек            | Почти в реальном времени |
| **base**   | ~10–20 сек           | Комфортно для заметок    |
| **small**  | ~25–40 сек           | Хороший баланс           |
| **medium** | ~60–90 сек           | Высокая точность         |
| **large**  | 2–4 мин              | Только для мощных ПК     |

---

Whisper бывает нескольких размеров. Чем больше модель — тем выше точность, но тем выше требования к железу.

### 📌 Таблица моделей Whisper

| Модель     | Размер  | Плюсы                           | Минусы                | Железо              |
| ---------- | ------- | ------------------------------- | --------------------- | ------------------- |
| **tiny**   | ~75 MB  | Очень быстрая, минимум ресурсов | Низкая точность       | CPU, 2 GB RAM       |
| **base**   | ~140 MB | Хороший баланс                  | Ошибки в длинной речи | CPU, 4 GB RAM       |
| **small**  | ~460 MB | Хорошая точность                | Медленнее             | CPU / GPU, 6 GB RAM |
| **medium** | ~1.5 GB | Высокая точность                | Требует мощный ПК     | GPU, 8–10 GB VRAM   |
| **large**  | ~3 GB   | Максимальная точность           | Очень тяжёлая         | GPU, 12+ GB VRAM    |

---

## 🖥️ Требования к железу (подробно)

Ниже приведены **минимальные и рекомендуемые требования** в зависимости от сценария использования.

---

### 💻 Минимальная конфигурация (работает у всех)

Подходит для:

* коротких аудио;
* Whisper `tiny` / `base`;
* лёгких моделей Ollama (`phi`, `gemma`).

**CPU:** 4 ядра (Intel i5 8th gen / Ryzen 5 2600)
**RAM:** 8 GB
**GPU:** не требуется
**Диск:** 10 GB свободного места
**OS:** Windows 10 / Linux / macOS

⚠️ Транскрибация будет медленной, но стабильной.

---

### ⚖️ Рекомендуемая конфигурация (оптимальный вариант)

Подходит для:

* Whisper `small`;
* лекций, интервью;
* моделей Ollama `mistral`, `llama3`.

**CPU:** 6–8 ядер (Intel i7 / Ryzen 5 5600X)
**RAM:** 16 GB
**GPU:** опционально (NVIDIA 6–8 GB VRAM)
**Диск:** SSD, 20–30 GB

✅ Хороший баланс скорость / качество.

---

### 🚀 Продвинутая конфигурация (высокая точность)

Подходит для:

* Whisper `medium`;
* длинных записей;
* сложных запросов к LLM.

**CPU:** 8+ ядер (Ryzen 7 / Intel i9)
**RAM:** 32 GB
**GPU:** NVIDIA RTX 3060 (12 GB VRAM) или лучше
**Диск:** SSD NVMe, 40+ GB

⚡ Быстрая транскрибация и комфортная работа.

---

### 🧠 Максимальная конфигурация (профи / продакшен)

Подходит для:

* Whisper `large`;
* больших объёмов аудио;
* тяжёлых LLM (`llama3` с большим контекстом).

**CPU:** 12+ ядер
**RAM:** 64 GB
**GPU:** RTX 3090 / 4090 (24 GB VRAM)
**Диск:** NVMe SSD, 50+ GB

🎯 Максимальная точность и скорость.

---

### 📌 Примечания

* NVIDIA GPU предпочтительнее (CUDA)
* AMD GPU поддерживаются хуже
* На CPU лучше использовать `faster-whisper`
* Ollama активно использует RAM, даже без GPU

---

## 🎯 Пресеты по умолчанию (рекомендуется)

### 👶 Новичку (поставил и работает)

* **Whisper:** `small`
* **Ollama:** `mistral`
* **Режим:** CPU / faster-whisper

✔ Хорошая точность
✔ Не требует GPU
✔ Подходит для ноутбуков

---

### ⚖️ Баланс качества и скорости

* **Whisper:** `medium`
* **Ollama:** `llama3`
* **Режим:** GPU (если есть)

✔ Отлично для лекций и интервью

---

### 🚀 Максимум качества

* **Whisper:** `large`
* **Ollama:** `llama3`
* **Режим:** GPU 12–24 GB VRAM

✔ Максимальная точность

---

## 🧠 CPU vs GPU: реальные отличия

| Критерий | CPU            | GPU                |
| -------- | -------------- | ------------------ |
| Запуск   | Работает везде | Нужна NVIDIA       |
| Скорость | Медленнее      | В 5–10 раз быстрее |
| Whisper  | tiny–small     | medium–large       |
| Ollama   | Да (RAM)       | Да (VRAM)          |
| Ноутбуки | Отлично        | Ограничено         |

**Вывод:**

* Без GPU обязательно использовать `faster-whisper`
* GPU оправдан для `medium` и `large`

---

## 💻 Ноутбуки vs ПК

### 💼 Ноутбук

* CPU: 4–8 ядер
* RAM: 16 GB
* Whisper: `base` / `small`
* Ollama: `phi`, `mistral`

⚠️ Избегать перегрева и `large`

---

### 🖥️ Настольный ПК

* CPU: 8+ ядер
* RAM: 32 GB
* GPU: RTX 3060+
* Whisper: `medium` / `large`
* Ollama: `llama3`

---

### 🔧 Рекомендации

* 💻 **Слабый ноутбук / офисный ПК** → `tiny` или `base`
* 🧠 **Баланс скорость/качество** → `small`
* 🎧 **Лекции, интервью, шум** → `medium`
* 🎯 **Максимальная точность** → `large`

---

### ⚡ faster-whisper (рекомендуется)

`faster-whisper` — оптимизированная версия Whisper:

* быстрее в 2–4 раза;
* меньше потребляет памяти;
* отлично работает на CPU.

Рекомендуется для продакшена.

---

## 🤖 Модели Ollama (LLM)

Ollama позволяет запускать большие языковые модели **локально**, без интернета.

### 📌 Популярные модели

| Модель      | Плюсы                            | Минусы              | Железо    |
| ----------- | -------------------------------- | ------------------- | --------- |
| **llama3**  | Отличное качество, универсальная | Требует ресурсы     | 8+ GB RAM |
| **mistral** | Быстрая, компактная              | Хуже логика         | 6 GB RAM  |
| **gemma**   | Лёгкая, быстрая                  | Короткие ответы     | 6 GB RAM  |
| **phi**     | Очень лёгкая                     | Ограниченные знания | 4 GB RAM  |

---

### 🧠 Для чего подходят модели

* 💬 **Чат и ответы на вопросы** → `llama3`, `mistral`
* 📝 **Анализ текста / резюме** → `llama3`
* 🚀 **Слабое железо** → `phi`, `gemma`

---

### ⚙️ Примеры команд Ollama

```bash
ollama pull llama3
ollama run llama3
```

Можно заменить модель на любую другую.

---

* **Python 3.10+**
* **PyQt6** — графический интерфейс
* **Whisper (OpenAI / faster‑whisper)** — распознавание речи
* **Ollama** — локальная LLM (например: llama3, mistral, gemma)
* **FFmpeg** — обработка аудио (обязательно установлен)

---

## 📦 Установка

### 1. Клонирование проекта

```bash
git clone https://github.com/yourname/yourproject.git
cd yourproject
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

Пример `requirements.txt`:

```text
pyqt6
openai-whisper
sounddevice
numpy
ollama
```

### 3. Установка FFmpeg

**Windows:**

* скачать с [https://ffmpeg.org](https://ffmpeg.org)
* добавить в PATH

**Linux:**

```bash
sudo apt install ffmpeg
```

**macOS:**

```bash
brew install ffmpeg
```

---

## ⚙️ Настройка Ollama

1. Установить Ollama: [https://ollama.com](https://ollama.com)
2. Скачать модель:

```bash
ollama pull llama3
```

3. Ollama должен быть запущен в фоне:

```bash
ollama serve
```

---

## ▶️ Запуск приложения

```bash
python main.py
```

После запуска откроется окно приложения.

---

## 🧩 Как работает приложение

1. Пользователь:

   * записывает голос **или** загружает аудиофайл
2. Аудио передается в **Whisper**
3. Whisper возвращает текст
4. Текст:

   * отображается в интерфейсе
   * может быть отправлен в **Ollama**
5. LLM формирует ответ и возвращает его в чат

---

## 💬 Примеры использования

* Расшифровка лекций и интервью
* Ответы на вопросы по аудиозаписи
* Анализ голосовых заметок
* Локальный AI‑ассистент без интернета

---

## ❗ Частые проблемы при установке и запуске

### 1. Whisper не устанавливается или падает с ошибкой

**Причины:**

* старая версия Python;
* нет FFmpeg;
* проблемы с PyTorch.

**Решение:**

* убедись, что Python версии **3.10+**:

```bash
python --version
```

* проверь, что FFmpeg установлен:

```bash
ffmpeg -version
```

* при проблемах попробуй установить faster-whisper:

```bash
pip install faster-whisper
```

---

### 2. FFmpeg не найден (Windows)

**Ошибка:** `ffmpeg not found` или `No such file or directory`

**Решение:**

* скачай FFmpeg с официального сайта;
* распакуй архив;
* добавь путь к папке `bin` в переменную среды **PATH**;
* перезапусти терминал.

---

### 3. Ollama не отвечает

**Ошибка:** `connection refused` или модель не запускается

**Решение:**

* убедись, что Ollama запущен:

```bash
ollama serve
```

* проверь список моделей:

```bash
ollama list
```

* если модели нет, скачай её:

```bash
ollama pull llama3
```

---

### 4. Нет звука или запись с микрофона не работает

**Причины:**

* неверное устройство ввода;
* нет прав на микрофон;
* конфликт драйверов.

**Решение:**

* проверь микрофон в системе;
* укажи устройство явно в `sounddevice`;
* на Windows/macOS дай приложению доступ к микрофону.

---

### 5. PyQt6 не запускается

**Ошибка:** приложение сразу закрывается или GUI не появляется

**Решение:**

* переустанови PyQt6:

```bash
pip uninstall pyqt6
pip install pyqt6
```

* убедись, что запуск происходит не из headless-среды (например, SSH без X-сервера).

---

## 🛠️ Возможные улучшения

* выбор модели Whisper и LLM из интерфейса
* потоковая транскрибация
* сохранение истории чата
* экспорт текста в файл
* поддержка нескольких языков

---


